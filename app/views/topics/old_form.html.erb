<%= form_for @topic do |t| %>
  <% if @topic.errors.any? %>
    <div id ="error_explanation">
      <h2>
        <%=pluralize(@topic.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @topic.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= t.label :title %><br>
    <%= t.text_field :title %>
  </p>

  <p>
    <%= t.label :author %><br>
    <%= t.text_field :author %>
  </p> 

  <p>
    <%= t.label :content %><br>
    <%= t.text_area :content %>
  </p>

  <p>
    <%= t.submit%>
  </p>
<% end %>
